# vex-iotbox

物联网小盒子，基于 ESP32 的 IoT 设备系统，支持设备自组网通信、移动端控制、云端采集与数据可视化。

## 系统架构

三个模块各自独立运行，通过网络协议互联互通：

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│                 │  MQTT   │                 │  HTTP   │                 │
│    硬件 ESP32   │◄───────►│   Java 服务端   │◄───────►│  Flutter App    │
│   (独立运行)    │         │   (独立运行)    │         │  (独立运行)     │
│                 │         │                 │         │                 │
└─────────────────┘         └─────────────────┘         └─────────────────┘
        ▲                                                   │
        │           WiFi / 蓝牙 (近场直连)                    │
        └───────────────────────────────────────────────────┘
                                     │
                                     │ JDBC
                                     ▼
                            ┌─────────────────┐
                            │    Metabase     │
                            │    报表平台     │
                            └─────────────────┘
```

- **硬件 ESP32** — 可脱离服务端和 App 单独运行，传感器数据本地处理
- **Flutter App** — 可通过蓝牙/WiFi 直连 ESP32，无需服务端参与
- **Java 服务端** — 通过 MQTT 采集和控制设备，提供 REST API；Metabase 接入数据库生成报表

## 三大模块

### 1. 硬件（ESP32）

- 主控芯片：ESP32-S3
- 设备间通过 WiFi + MQTT 通信
- 通过 PlatformIO + Arduino 开发

### 2. 移动端 App（Flutter）

- 蓝牙（BLE）近场控制 ESP32
- WiFi 直连 ESP32 通信
- 通过云端 API 远程控制与状态查看

### 3. 云端服务器（Java）

- Spring Boot 单体架构（不使用微服务）
- Spring AI + MQTT 集成
- Metabase 报表，数据可视化与统计分析

## 目录结构

```
vex-iotbox/
├── hardware/          # ESP32 固件代码（PlatformIO）
├── app/               # Flutter 移动端应用
└── server/            # Java 云端服务
```

## 快速开始

各模块独立开发，分别进入对应目录查看各自的启动说明：

- **硬件：** `hardware/README.md`
- **App：** `app/README.md`
- **服务端：** `server/README.md`

## License

MIT
